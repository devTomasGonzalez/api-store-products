package com.overactive.customer.api.repository;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class ProductRepositoryTest {

	/*
	@Autowired
	private ProductRepository productRepository;

	@Test
	public void addValidTransactionTest() throws Exception {
		Product c = new Product();
		c.setCustomerLastName("HELEN");
		c.setCustomerName("MICHAEL");
		c.setListTransactions(null);

		Transaction t = new Transaction();
		t.setAmount(100);
		t.setCustomer(c);
		t.setProductDesc("Ipod");

		customerRepository.save(c);
		assertNotNull(productRepository.findById(4L));

		productRepository.save(t);
		assertNotNull(productRepository.findById(10L));

	}
	
	@Test
	public void updateValidTransactionTest() throws Exception {
		Product c = new Product();
		c.setCustomerLastName("HELEN");
		c.setCustomerName("MICHAEL");
		c.setListTransactions(null);

		Transaction t = new Transaction();
		t.setAmount(100);
		t.setCustomer(c);
		t.setProductDesc("Ipod");

		customerRepository.save(c);
		assertNotNull(productRepository.findById(4L));

		productRepository.save(t);
		assertNotNull(productRepository.findById(10L));

	}
	*/
		
}
